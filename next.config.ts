import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.squarespace-cdn.com/'], // el dominio de la imagen externa
  },
};

export default nextConfig;
